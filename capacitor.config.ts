import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'numbersfight',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
